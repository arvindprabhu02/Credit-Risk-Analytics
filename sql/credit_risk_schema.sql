CREATE TABLE loan_customers (
    ID INTEGER,
    Age INTEGER,
    Experience INTEGER,
    Income INTEGER,
    ZIPCode INTEGER,
    Family INTEGER,
    CCAvg REAL,
    Education INTEGER,
    Mortgage INTEGER,
    Securities_Account INTEGER,
    CD_Account INTEGER,
    Online INTEGER,
    CreditCard INTEGER,
    Personal_Loan INTEGER
);
